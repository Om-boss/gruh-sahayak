function card(w){return `<article class="worker-card"><div class="worker-top"><div class="worker-avatar">${w.initials}</div><div><h3>${w.name} <small>✓ Verified</small></h3><p>📍 ${w.area}, ${w.city}</p><p>⭐ ${w.rating} · ${w.experience} yrs</p></div></div><div class="worker-meta">${w.services.map(s=>`<span>${s}</span>`).join("")}<span>₹${w.price}/day</span></div><div class="worker-actions"><a href="tel:+${w.phone}">📞 Call</a><a target="_blank" href="https://wa.me/${w.phone}">💬 WhatsApp</a><a href="worker-profile.html?id=${w.id}">Profile</a></div></article>`}
const featured=document.getElementById("featuredWorkers");if(featured)featured.innerHTML=workers.slice(0,3).map(card).join("");
document.getElementById("homeSearch")?.addEventListener("submit",e=>{e.preventDefault();let p=new URLSearchParams();let a=document.getElementById("homeArea").value,s=document.getElementById("homeService").value;if(a)p.set("area",a);if(s)p.set("service",s);location.href="workers.html?"+p});
document.querySelector(".menu-toggle")?.addEventListener("click",()=>document.querySelector(".nav-links").classList.toggle("show"));